# zipax v0.25.3

zipax v0.25.3 是补丁版本，修复 Windows 开机自启动路径问题，并修复 Linux 安装包依赖与命名问题。

## 更新内容

- 修复 Windows 开机自启动失败：清除注册表路径中的 `\\?\` 前缀和尾部空格。
- 修复 Linux deb/rpm 依赖缺失：补全 `libde265-0` 依赖，解决安装后可执行文件无法启动（缺少 `libde265.so.0`）的问题。
- 统一 Linux 命令行二进制/命令名为 `zipax`（此前为 `zipax-app`），与包名保持一致性。
- 代码质量改进和小修复。
